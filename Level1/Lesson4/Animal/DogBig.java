public class DogBig extends dog {

    public boolean run(double run){
        return (run > 400 && run <= 600);
    }
    public boolean swim(double swim) {
        return (swim > 5 && swim <= 10);
    }
}
